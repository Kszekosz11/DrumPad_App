<Window x:Class="SoundsTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoundsTest"        
        mc:Ignorable="d"
        xmlns:samples="clr-namespace:SoundsTest"        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:WPFSoundVisualizationLib1="clr-namespace:WPFSoundVisualizationLib;assembly=WPFSoundVisualizationLib"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow" Height="600" Width="400" MinHeight="600" MinWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox>
            <GroupBox.Header>
                <TextBlock Text="Drums" FontSize="12" Height="12" TextAlignment="Center"/>
            </GroupBox.Header>
            <samples:UserControlSamples DataContext="{Binding DrumsSamples}" Background="AliceBlue"/>
        </GroupBox>

        <GroupBox Grid.Column="1">
            <GroupBox.Header>
                <TextBlock Text="Guitar" FontSize="12" Height="12" TextAlignment="Center"/>
            </GroupBox.Header>
            <samples:UserControlSamples Tag="1" Background="AntiqueWhite" DataContext="{Binding GuitarSamples}"/>
        </GroupBox>

        <GroupBox Grid.Column="0" Grid.Row="1">
            <GroupBox.Header>
                <TextBlock Text="Flutes" FontSize="12" Height="12" TextAlignment="Center"/>
            </GroupBox.Header>
            <samples:UserControlSamples Tag="2" Background="Aqua" DataContext="{Binding FlutesSamples}"/>
        </GroupBox>

        <GroupBox Grid.Column="1" Grid.Row="1">
            <GroupBox.Header>
                <TextBlock Text="Piano" FontSize="12" Height="12" TextAlignment="Center"/>
            </GroupBox.Header>
            <samples:UserControlSamples Tag="3" Background="Aquamarine" DataContext="{Binding PianoSamples}"/>
        </GroupBox>

        <GroupBox Grid.Column="0" Grid.Row="2">
            <GroupBox.Header>
                <TextBlock Text="Vox" FontSize="12" Height="12" TextAlignment="Center"/>
            </GroupBox.Header>
            <samples:UserControlSamples Tag="4" Background="Azure" DataContext="{Binding VoxSamples}"/>
        </GroupBox>

        <GroupBox Grid.Column="1" Grid.Row="2">
            <GroupBox.Header>
                <TextBlock Text="FX" FontSize="12" Height="12" TextAlignment="Center"/>
            </GroupBox.Header>
            <samples:UserControlSamples Tag="5" Background="Beige" DataContext="{Binding FXSamples}"/>
        </GroupBox>
    </Grid>
</Window>